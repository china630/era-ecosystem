# План работ на 2026-06-05 — ERA Ecosystem

## Статус стабилизации (integration/stabilize-20260605)

| Область | Статус | Примечание |
|---------|--------|------------|
| Миграции БД | OK в Docker | `era-postgres` + контейнеры; host-migrate требует URL-encoded пароль в `DATABASE_URL` |
| Ночная проверка | Исправлено | `IMAGE_TAG` = ветка (`master`/`dev`); fallback `docker compose build` при отсутствии GHCR |
| CI data-hub | +тесты | `jest` + health spec |
| Orchestrator tests | Исправлен | `hotel-module-entitlements` — 11 ключей |
| gh CLI | Блокер | winget: ошибка сертификата; нужна ручная установка + `gh auth login` |
| npm audit all | Долго | скрипт `scripts/npm-audit-all.mjs`; прогнать по модулям точечно |

## P0 — Завершить отгрузку в dev

1. Установить [GitHub CLI](https://cli.github.com/) → `gh auth login` → `gh pr create` в `dev`.
2. Дождаться зелёного **CI** и **Build and push images** на PR.
3. Проверить **Nightly smoke** (manual `workflow_dispatch` после merge образов).
4. При падении CI — цикл: `gh run view --log-failed` → фикс → покоммит в нужный модуль → push.

## P1 — Тестовое покрытие

| Модуль | Задача |
|--------|--------|
| era-data-hub | Registry service unit tests, интеграция с finance catalog |
| era-hotel-pms | Playwright e2e в отдельном job; co-located specs в CI |
| era-clinic | Покрыть sanatorium/scheduling services (уже есть specs — держать зелёными) |
| era-finance-core | Network/orchestrator integration tests в CI DB |
| Сателлиты | Минимум 1 domain spec на модуль (не только api-health) |

## P2 — Безопасность и зависимости

- Довести `npm audit` остаток (41 в finance-core) — отчёт `docs/security/AUDIT-*.md`.
- Не применять `audit fix --force` без регрессионного прогона.
- Обновить lockfile только там, где audit fix затронул дерево.

## P3 — Документация (расхождения)

- [docs/READINESS_MATRIX.md](../READINESS_MATRIX.md) — обновить после merge (скилл era-readiness-matrix).
- [docs/CI_CD.md](../CI_CD.md) — nightly: `IMAGE_TAG` по ветке, fallback build.
- [era-finance-core/docs/plans/4_orchestrator.md](../../era-finance-core/docs/plans/4_orchestrator.md) — manual QA network/FIN.
- Сателлиты: `*/doc/DELIVERY-*.md` vs фактические API routes.
- [docs/ECOSYSTEM_URLS.md](../ECOSYSTEM_URLS.md) — data-hub `:4200`, health `/health` и `/healthz`.

## P4 — Продуктовые волны (после зелёного CI)

1. **Finance** — завершить reconciliation UI для ERA network netting.
2. **Orchestrator** — `OrganizationDeployment` routing table (4_orchestrator Part C option B).
3. **Hotel** — FO waves ElectraWeb (планы в `.cursor/plans/`).
4. **Clinic** — sanatorium vNext (docs/SANATORIUM-VNEXT-PLAN.md).

## P5 — Инфраструктура

- Проверить GHCR теги `dev` и `master` после каждого merge в соответствующую ветку.
- Staging deploy (`deploy-staging.yml`) — smoke после pull.
- Локально: `POSTGRES_PASSWORD` с `!@` — всегда `encodeURIComponent` в URL (bootstrap исправлен).

## Команды быстрого старта

```powershell
cd "d:\My Projects\era-ecosystem"
docker compose up -d postgres redis
$env:POSTGRES_PASSWORD = (Get-Content .env | Where-Object { $_ -match '^POSTGRES_PASSWORD=' }) -replace 'POSTGRES_PASSWORD=',''
node tools/bootstrap-local.mjs --migrate-satellites

cd era-orchestrator; npm run test:api; npm run build
cd ..\era-finance-core; npm run build -w @erafinance/api
cd ..\era-data-hub; npm run test -w @era/data-hub-api; npm run build
```
