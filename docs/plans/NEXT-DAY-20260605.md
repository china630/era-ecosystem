# План работ на 2026-06-05 — ERA Ecosystem

> Composer-планы `era-finance-core/docs/plans/*` удалены 2026-06-05 — реализовано и зафиксировано в [PRD](../../era-finance-core/PRD.md) / [TZ](../../era-finance-core/TZ.md); manual QA: `era-finance-core/docs/manual-qa/`.

## Статус (после merge PR #7 → `dev`)

| Область | Статус |
|---------|--------|
| Стабилизация в `dev` | ✅ PR [#7](https://github.com/china630/era-ecosystem/pull/7) merged |
| CI (packages, orchestrator, finance, data-hub, сателлиты) | ✅ зелёный |
| Лишние PR (#6, #8) | ✅ закрыты |
| gh CLI | PATH настроен; `GH_TOKEN` из git credential работает |
| Finance waves 1–4 | ✅ в коде + PRD §4.17–4.19, TZ §27–29 |

## P0 — Сегодня

1. **Локально перейти на `dev`:** `git checkout dev && git pull` (убрать мешающие untracked `era-finance-core/Dockerfile*` при необходимости).
2. **Build images** — дождаться/запустить workflow образов на `dev` после merge.
3. **Nightly smoke** — `workflow_dispatch` на `dev` (или после merge в `master` с правильным `IMAGE_TAG`).
4. **Manual QA finance** — [UX_WAVE_1_2_CORE.md](../../era-finance-core/docs/manual-qa/UX_WAVE_1_2_CORE.md), [UX_WAVE_4_ORCHESTRATOR.md](../../era-finance-core/docs/manual-qa/UX_WAVE_4_ORCHESTRATOR.md).

## P1 — Тестовое покрытие

| Модуль | Задача |
|--------|--------|
| era-data-hub | Registry service unit tests, интеграция с finance catalog |
| era-hotel-pms | Playwright e2e в отдельном job |
| era-finance-core | E2E multi-finance (отложено в TZ §29.8) |
| Сателлиты | ≥1 domain spec на модуль |

## P2 — Безопасность

- Остаток `npm audit` — [AUDIT-20260605.md](../security/AUDIT-20260605.md).
- Без `audit fix --force` без регрессии.

## P3 — Документация

- [READINESS_MATRIX.md](../READINESS_MATRIX.md) — обновить (скилл era-readiness-matrix).
- [CI_CD.md](../CI_CD.md) — nightly `IMAGE_TAG` по ветке.
- Сателлиты: `*/doc/DELIVERY-*.md` vs фактические routes.

## P4 — Продукт (после зелёных образов)

1. Finance — reconciliation UI для ERA network netting (PRD §4.19).
2. Orchestrator — `OrganizationDeployment` routing (TZ §29.8 option B).
3. Hotel — FO waves ElectraWeb.
4. Clinic — sanatorium vNext (`docs/SANATORIUM-VNEXT-PLAN.md`).

## P5 — Релиз в master (когда готов)

- Отдельный PR **`dev` → `master`** (не дублировать integration напрямую в master).

## Быстрый старт

```powershell
cd "d:\My Projects\era-ecosystem"
git checkout dev
git pull origin dev
docker compose up -d postgres redis
node tools/bootstrap-local.mjs --migrate-satellites
```
