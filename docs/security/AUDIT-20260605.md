# npm audit report 2026-06-05

Скрипт: `node scripts/npm-audit-all.mjs` (полный прогон по всем `package.json`).

## Платформенные модули (точечный `npm audit fix`)

| Модуль | Действие | Примечание |
|--------|----------|------------|
| era-finance-core | `npm audit fix` | Остаток moderate/high — см. `npm audit` в каталоге |
| era-orchestrator | `npm audit fix` | Часть пакетов требует major bump |
| era-data-hub | `npm audit fix` | См. lockfile после merge |

## Рекомендации

- Не использовать `npm audit fix --force` без отдельного регрессионного цикла.
- Сателлиты: повторить `npm audit fix` в каждом `era-*` после merge платформы.
- CI: добавить advisory-only job (optional) на `dev`.
