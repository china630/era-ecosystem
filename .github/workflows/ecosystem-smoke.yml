# DEPRECATED — use .github/workflows/ci.yml (tests) and build-images.yml (GHCR).
# Kept temporarily so open PRs are not surprised; safe to delete after branch protection uses ci.yml.

name: Ecosystem smoke (deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use ci.yml and build-images.yml instead."
