---
description: ERA Data Hub — no web UI (API-only)
globs:
  - apps/api/**
alwaysApply: false
---

# Data Hub UI

This app has **no Next.js web layer**. API-only (`apps/api`). UI changes belong in finance or orchestrator admin if needed; document API contracts in `docs/adr/era-data-hub.md`.
