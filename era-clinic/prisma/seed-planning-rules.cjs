"use strict";
/**
 * Moved to seed-planning-rules.ts (needs TypeScript helper).
 * Use: npm run db:seed:planning-rules
 */
console.error(
  "[seed-planning-rules] use `npx tsx prisma/seed-planning-rules.ts` or npm run db:seed:planning-rules",
);
process.exit(1);
