---
description: ERA Auto Service — local dev port 3208, Prisma, satellite env
alwaysApply: true
---

# Auto Service — local development

**Inherits:** monorepo root `.cursor/rules/`. Port **3208** — `npm run dev`.

Prisma: `prisma/schema.prisma`. **Env:** standard satellite vars.

**i18n:** `messages/{az,ru,en}.json` + `@era/i18n-common`.
