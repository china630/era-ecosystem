{
  "name": "@era/clinic-domain",
  "version": "0.1.0",
  "private": true,
  "description": "Pure clinic domain helpers (patient, presets, inpatient ADT)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest --ci"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.3"
  }
}
