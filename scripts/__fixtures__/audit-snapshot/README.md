# Audit snapshot fixtures

Optional mini-repo stubs for integration audit unit tests.

Baseline logic tests live in `scripts/__tests__/integration-audit.spec.mjs` and use in-memory fixtures.

Future: add fake app trees here to test grep rules without scanning the full monorepo.
