---
description: ERA Construction — local dev port 3206, Prisma, satellite env
alwaysApply: true
---

# Construction — local development

**Inherits:** monorepo root `.cursor/rules/`. Port **3206** — `npm run dev`.

Prisma: `prisma/schema.prisma`. **Env:** standard satellite vars.

**i18n:** `messages/{az,ru,en}.json` + `@era/i18n-common`.
