# Traefik dynamic routing — era-network (see docs/ECOSYSTEM_URLS.md).
# Finance API public host: finance-api.era-365.online (same :4100; gate at edge if needed).

http:
  routers:
    # HTTP→HTTPS for all hosts except ACME HTTP-01 challenge path.
    http-to-https:
      rule: "HostRegexp(`{host:.+}`) && !PathPrefix(`/.well-known/acme-challenge/`)"
      entryPoints:
        - web
      middlewares:
        - redirect-to-https
      service: noop
      priority: 1

    apex-redirect:
      rule: Host(`era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      middlewares:
        - apex-to-app
      service: orchestrator-web
      priority: 100

    orchestrator-app:
      rule: Host(`app.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: orchestrator-web

    orchestrator-api:
      rule: Host(`api.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: orchestrator-api

    finance-web:
      rule: Host(`finance-core.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: finance-web

    finance-api:
      rule: Host(`finance-api.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: finance-api

    data-hub:
      rule: Host(`data.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: data-hub

    hotel-pms:
      rule: Host(`hotel-pms.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: hotel-pms

    fnb-pos:
      rule: Host(`fnb-pos.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: fnb-pos

    retail-pos:
      rule: Host(`retail-pos.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: retail-pos

    logistics:
      rule: Host(`logistics.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: logistics

    construction:
      rule: Host(`construction.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: construction

    crm:
      rule: Host(`crm.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: crm

    auto-service:
      rule: Host(`auto-service.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: auto-service

    wholesale:
      rule: Host(`wholesale.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: wholesale

    clinic:
      rule: Host(`clinic.era-365.online`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: clinic

  middlewares:
    redirect-to-https:
      redirectScheme:
        scheme: https
        permanent: true

    apex-to-app:
      redirectRegex:
        regex: "^https?://era-365\\.online/(.*)"
        replacement: "https://app.era-365.online/${1}"
        permanent: true

  services:
    # Dummy backend for the HTTP redirect router (middleware short-circuits).
    noop:
      loadBalancer:
        servers:
          - url: "http://127.0.0.1"

    orchestrator-web:
      loadBalancer:
        servers:
          - url: "http://orchestrator:3000"

    orchestrator-api:
      loadBalancer:
        servers:
          - url: "http://orchestrator:4000"

    finance-web:
      loadBalancer:
        servers:
          - url: "http://finance-web:3100"

    finance-api:
      loadBalancer:
        servers:
          - url: "http://finance-core:4100"

    data-hub:
      loadBalancer:
        servers:
          - url: "http://data-hub:4200"

    hotel-pms:
      loadBalancer:
        servers:
          - url: "http://hotel-pms:3201"

    fnb-pos:
      loadBalancer:
        servers:
          - url: "http://fnb-pos:3202"

    retail-pos:
      loadBalancer:
        servers:
          - url: "http://retail-pos:3204"

    logistics:
      loadBalancer:
        servers:
          - url: "http://logistics:3205"

    construction:
      loadBalancer:
        servers:
          - url: "http://construction:3206"

    crm:
      loadBalancer:
        servers:
          - url: "http://crm:3207"

    auto-service:
      loadBalancer:
        servers:
          - url: "http://auto-service:3208"

    wholesale:
      loadBalancer:
        servers:
          - url: "http://wholesale:3209"

    clinic:
      loadBalancer:
        servers:
          - url: "http://clinic:3203"
