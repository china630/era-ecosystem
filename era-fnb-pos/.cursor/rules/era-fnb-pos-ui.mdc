---
description: F&B POS — UI per satellite playbook
globs:
  - app/**/*.tsx
  - src/**
alwaysApply: false
---

# F&B POS UI

Follow **`docs/UI_PLAYBOOK_SATELLITES.md`**. POS floor UX: fast ticket/table flows, KDS-friendly actions.

Use `@era/satellite-kit/ui` shared components. Waiter/manager roles: `FB_WAITER`, `FB_MANAGER` — hide platform billing unless SSO session with `financeRole`.
