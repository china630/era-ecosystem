# era-fnb-pos — module map

| Route | API | Role |
|-------|-----|------|
| `/floor` | tickets, tables, outlets | Waiter — walk-in, banquet BEO |
| `/orders` | tickets, in-house link, room-charge, pay cash/card, `defer-to-hub`, shift panel | Waiter / manager |
| `/kds` | kds fire | Kitchen |
| `/admin/daily-menu` | `/api/admin/daily-menu` | Manager |
| `/admin/settings` | outlet config | Admin |

Integration: `GET /api/in-house`, `GET /api/banquets`, `GET /api/billing/context`, PMS room-charge + settlement pending bridge (`POST /api/tickets/[id]/defer-to-hub`, `POST /api/integration/settlement-confirmed`).
