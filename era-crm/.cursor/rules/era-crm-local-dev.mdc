---
description: ERA CRM — local dev port 3207, Prisma, satellite env
alwaysApply: true
---

# CRM — local development

**Inherits:** monorepo root `.cursor/rules/`. Port **3207** — `npm run dev`.

Prisma: `prisma/schema.prisma`. **Env:** standard satellite vars.

**i18n:** `messages/{az,ru,en}.json` + `@era/i18n-common`.
