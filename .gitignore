node_modules/
**/npm-cache/
era-finance-core/D*/
.env
.env.local
.env*.local
era.droplet.env
*.log
# Stage-gate raw logs (commit *-signoff.md only)
reports/*.log
reports/*-e2e.log
.DS_Store
dist/
**/.next/
**/dist/
*.tsbuildinfo

# Local dev credentials (never commit)
tmp/era-local-credentials.md

# Docker persistent data (never commit)
docker-data/

# Generated at satellite image build / migrate-all host baseline (never commit)
**/prisma/baseline.sql

# Satellite runtime org bind file cache
**/.data/

# IDE / local plans (skills + rules under .cursor/ are tracked)
.cursor/plans/

# Legal entity enrichment cache + master DB (local; never commit — backup to D:\ERA-BACKUP)
data/legal-entities/.cache/
data/legal-entities/azerbaijan-companies-with-voen.csv
data/legal-entities/azerbaijan-companies-without-voen.csv
data/legal-entities/.companies-master-stats.json
data/legal-entities/.token-seeds-*.json
data/legal-entities/.token-seeds.lock
data/legal-entities/.trigram-sweep*.json
data/legal-entities/.trigram-sweep.lock

# Elektraweb bridge HAR / PII captures (LoginToken + guest data — never commit)
**/*.har
era-hotel-pms/doc/nafta/bridge-har/_*.txt
era-hotel-pms/doc/nafta/bridge-har/*.json

# Local port/reference scratch (never commit)
.cursor/port-refs/


# Local git worktrees (never commit as gitlinks)
.wt-*/

# Local-only JWT signing material (generate per env; never commit private d)
**/dev-rs256-jwk.json
**/dev-*-jwk.json

# Local e-taxes enrichment scratch (never commit)
data/legal-entities/.etaxes-token-hypotheses.json
data/legal-entities/.token-seeds-*.json

# WebOnly clinic cutover dumps (patient PII — never commit; keep under D:\ERA-BACKUP)
data/nafta-webonly-dumps/

